# Vendored from spine/generated/python/result.py — re-sync via spine/scripts/sync-mbrl.sh
# GENERATED by spine-codegen — DO NOT EDIT.
# Source of truth: spine/src/Spine/Wire.hs + Spine/Schema.hs
from dataclasses import dataclass, field, asdict
from typing import Any, Optional

STATUS = ("pass", "fail", "partial", "refuted-confirmed", "open", "skip", "error",)

@dataclass
class ResultEnvelope:
    id: str
    task: str
    status: str
    ts: str
    seed: Optional[int] = None
    params: dict = field(default_factory=dict)
    value: Optional[Any] = None
    evidence: dict = field(default_factory=dict)
    blockers: list = field(default_factory=list)
    method: Optional[str] = None
    checked: Optional[str] = None

    def to_dict(self) -> dict:
        return {k: v for k, v in asdict(self).items()}
