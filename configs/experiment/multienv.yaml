# @package _global_
# Validation item 1 — multi-environment replication, the breadth gap.
# Run as W&B sweep over env x seed on Colab GPU (Mode A).
experiment:
  name: multienv
training:
  total_env_steps: 1000000
